# 原始附件使用约定

本阶段不复制也不修改题目附件。程序默认只读引用同一工作区中的：

- `../../../A题/附件/附件1.xlsx`
- `../../../A题/附件/附件2.xlsx`

路径由仓库相对位置解析，不把个人电脑绝对路径写入代码。加载时会校验工作表、列数、记录数、时间范围、步长、缺失值、有限性与半径单调性，并计算 SHA-256 写入后续运行元数据。若仓库被单独移动，可向加载函数显式传入附件路径。

`data/raw/` 视为只读区域；任何清洗或派生数据应写入 `data/processed/`。
